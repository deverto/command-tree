
TODO before release v1.0.0
==========================

- enh:

    - "global" argument (in mutex groups too)

- cleanup:

    - refactor exceptions to sg tree like stuff

    - remove all TODO from source

    - logging!!

- doc

    - add docstring to all public class and function

    - generic doc:
        - why is better than the alternatives

    - docstring abs method ref

other issues, ideas
===================

- default value is list, but nargs is present, the type is not guessable
    @tree.argument(help = 'the hook args', nargs = '*', type = str) # TODO: the type is not that type

- API for get command tree  (or print)

- reusable arg "group" ??

- get arg type from docstring

    >>> from command_tree.types import valid_file
    >>>
    >>> def get_global_attr(attr_name):
    ...
    ...     data = globals()
    ...
    ...     if attr_name in data:
    ...         return data[attr_name]
    ...
    ...     builtin_attrs = data['__builtins__']
    ...
    ...     if hasattr(builtin_attrs, attr_name):
    ...         return getattr(builtin_attrs, attr_name)
    ...
    ...     return None
    ...
    >>> print(get_global_attr('valid_file'))
    <function valid_file at 0x7fbead13ab18>
    >>> print(get_global_attr('str'))
    <type 'str'>

    - python2-3 compat!
    - what about submodules? os.path.isfile, etc
